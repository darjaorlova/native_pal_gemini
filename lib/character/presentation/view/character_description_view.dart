import 'package:flutter/material.dart';

class CharacterDescriptionView extends StatelessWidget {
  const CharacterDescriptionView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
